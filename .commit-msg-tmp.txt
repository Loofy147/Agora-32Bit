feat(chat): view_image tool-result image transcription via the result row

view_image results declare transcribeImages and reuse the main image
transcription flow for one image per call, streamed as a thinking segment
with the singular Transcribing image label (12 locales). The description
reaches the model through the API-only image-context row and travels WITH
the result row (ToolCallData.transcription / MessageSegment.toolTranscription)
because the round-boundary path rebuild excludes the model message.

Also: transcription-enabled models never receive raw images on any
projection path (fixes the provider 400 unknown variant image_url);
the bottom sheet renders the Transcribing image state text in primary at
the plain body size; a detached background shell job no longer occupies the
group loading indicator (isActive excludes BACKGROUND_RUNNING); all other
colors remain unchanged.

Contract and unit tests updated across all areas.

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>
